<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uiuccourseexploler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage"></activity>
        <activity android:name=".MajorPage"></activity>
        <activity android:name=".CollegeList"></activity>
        <activity android:name=".CoursePage"></activity>
        <activity android:name=".Course"></activity>
        <activity android:name=".ProfessorPage"></activity>
        <activity android:name=".Uncscourse"></activity>
        <activity android:name=".Desclaimer"></activity>

    </application>

</manifest>